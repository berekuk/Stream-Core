# vim:ft=perl

use Stream::Filter qw(filter);
use warnings;
use strict;

sub {
    filter(sub {
        my $item = shift;
        $item =~ s{\\}{\\\\}g;
        $item =~ s{\n}{\\n}g;
        return "$item\n";
    });
}
