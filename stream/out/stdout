# vim:ft=perl

use parent qw(Stream::Out);
use warnings;
use strict;

use IO::Handle;

STDOUT->autoflush(1);

sub write {
    my ($class, $item) = @_;
    print $item;
}

sub {
    __PACKAGE__->new;
}
