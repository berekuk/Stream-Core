# vim:ft=perl

use base qw(Stream::In);
use warnings;
use strict;

sub read {
    my $self = shift;
    return if $self->{eof};
    my $item = scalar <STDIN>;
    if (eof STDIN) {
        $self->{eof}++;
    }
    return $item;
}

__PACKAGE__->new;

