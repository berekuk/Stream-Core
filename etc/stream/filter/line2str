# vim:ft=perl

use Stream::Filter qw(filter);
use warnings;
use strict;

filter(sub {
    my $item = shift;
    $item =~ s{(\\\\|\\n)}{
        ($1 eq "\\\\") ? "\\" : "\n"
    }e;
    return $item;
});
